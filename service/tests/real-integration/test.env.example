# Real Integration Tests Configuration
# Copy this file to test.env and configure your settings
# Note: This is only for TESTING, not for production deployment

# Required: Enable real integration tests
REAL_INTEGRATION_ENABLED=true

# Optional: Gemini model to use (default: gemini-2.5-flash)
# Note: Uses local gemini CLI, no API key required
GEMINI_MODEL=gemini-2.5-flash

# Optional: Test timeout in milliseconds (default: 60000)
TEST_TIMEOUT=120000

# Optional: Enable cleanup after tests (default: false)
REAL_INTEGRATION_CLEANUP=true

# Usage Instructions:
# 1. Copy this file: cp test.env.example test.env
# 2. Adjust settings as needed
# 3. Load before running tests: source test.env && npm run test:real-integration
# 
# Or alternatively, set environment variables directly:
# export REAL_INTEGRATION_ENABLED=true
# npm run test:real-integration 